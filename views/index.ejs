<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Employee</title>
    <link href="https://cdn.jsdelivr.net/npm/daisyui@4.12.12/dist/full.min.css" rel="stylesheet" type="text/css" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="navbar bg-base-100">
        <div class="flex-1">
          <a class="btn btn-ghost text-xl" href="/">Employee</a>
        </div>
        <div class="flex-none">
          <ul class="menu menu-horizontal px-1">
            <li><a href="/employees/add">Add Employee</a></li>
          </ul>
        </div>
      </div>

      <div class="flex justify-center my-4">
        <form action="/" method="get" class="flex gap-2">
            <input type="text" name="searchByName" placeholder="Search by name" class="input input-bordered">
            <input type="text" name="searchByAge" placeholder="or by age" class="input input-bordered">
            <button type="submit" class="btn btn-primary">Search</button>
        </form>
      </div>

      <div role="alert" class="alert alert-info">
        <svg
          xmlns="http://www.w3.org/2000/svg"
          fill="none"
          viewBox="0 0 24 24"
          class="h-6 w-6 shrink-0 stroke-current">
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
        </svg>
        <span>Jumlah seluruh employee adalah <%= stats.total_employees %>. Untuk employee tertua berumur <%= stats.oldest_employee %>, serta yang termuda berumur <%= stats.youngest_employee %></span>
      </div>


      <div class="flex flex-wrap justify-start">
        <% employee.forEach(el => { %>
            <div class="card bg-base-100 w-96 shadow-xl m-4">
                <figure>
                  <img
                    src="<%= el.profile_picture %>" alt="Shoes" style="width: 300px; height: 200px;"/>
                </figure>
                <div class="card-body">
                  <h2 class="card-title"><%= el.name %></h2>
                  <p><span><%= el.position %></span> | <span><%= el.age %></span> | <span><%= el.education %></span></p>
                  <div class="card-actions justify-end">
                    <button class="btn btn-primary" onclick="location.href='/employees/<%= el.id %>'">See Details</button>
                    <button class="btn btn-error" onclick="location.href='/employees/<%= el.id %>/delete'">Delete</button>
                  </div>
                </div>
              </div>
          <% }) %>
      </div>

      

      


</body>
</html>